name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build backend image
        run: |
          docker build -t bisrat1/projectmanagement-backend:latest ./backend
          docker push bisrat1/projectmanagement-backend:latest

      - name: Build frontend image
        run: |
          docker build -t bisrat1/projectmanagement-frontend:latest ./frontend
          docker push bisrat1/projectmanagement-frontend:latest

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3

      - name: Apply Kubernetes manifests
        run: |
          kubectl apply -f k8s/
