apiVersion: v1
kind: Pod
metadata:
  name: backend-test
  labels:
    app: backend-test
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:latest
      command: ["curl"]
      args: ["http://backend.dev.svc.cluster.local:8000/api/health"]

